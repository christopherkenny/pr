# Changelog

## pr (development version)

- Implements a `prob` vector and corresponding `prob_mat` and
  `prob_mat_cnt` record classes for representing probabilities and
  matrices of probabilities.
